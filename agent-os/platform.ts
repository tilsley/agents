/** Where the agent-os platform lives — the one URL from which everything else
 *  is discovered (the console's config.json is the well-known document). */
export const CONSOLE_URL = "https://d1x3uwxrkjekua.cloudfront.net";
